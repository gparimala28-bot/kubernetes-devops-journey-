apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: flask-k8s-demo
spec:
  selector:
    app: flask-k8s-demo

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5000

  type: {{ .Values.service.type }}