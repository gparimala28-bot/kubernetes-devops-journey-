apiVersion: v1
kind: Service
metadata:
  name: {{ include "employee-portal.fullname" . }}
  labels:
    {{- include "employee-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "employee-portal.selectorLabels" . | nindent 4 }}
